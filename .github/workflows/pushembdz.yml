name: PUSHEMBDZ Updater

on:
  schedule:
   - cron: '0 */1 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache Python packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install deps
        run: |
          pip install playwright httpx[http2] selectolax pytz
          playwright install chromium

      - name: Run updater
        run: python pushembdz.py

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Check if playlist files exist and have changes
          if [ -f "pushembdz.m3u8" ]; then
            git add pushembdz.m3u8
            if ! git diff --cached --quiet; then
              git commit -m "Update PUSHEMBDZ playlists [skip ci]"
              git push
              echo "Changes pushed successfully"
            else
              echo "No changes to commit"
            fi
          else
            echo "Playlist files not found"
            exit 1
          fi
